from django.conf.urls import patterns, include, url
from django.contrib import admin

urlpatterns = patterns('UrlShortener.core.views',
    # Examples:
    # url(r'^$', 'UrlShortener.views.home', name='home'),
    # url(r'^blog/', include('blog.urls')),
    
)
